# 삐약이 최종발표 — Part III 발표 대본 (구홍 · 기술 구현 및 시스템 아키텍처)

> 슬라이드 14~25. 구어체, 코드 검증 사실 기준. 전환 멘트 포함.

---

## 14. 시스템 아키텍처 전체 구조도

안녕하세요, 백엔드를 맡은 구홍입니다. 지금부터 삐약이의 기술 구현과 시스템 아키텍처를 설명드리겠습니다.

전체 구조는 크게 세 부분입니다. 사용자는 React Native 앱을 쓰고, 요청은 nginx를 거쳐 Spring Boot 서버로 들어옵니다. 이 서버 한 대 안에 애플리케이션·MySQL·Redis·모니터링까지 도커로 함께 올려 비용을 최소화했고, OCR·OpenAI·카카오·푸시 같은 외부 서비스와 연동됩니다. 배포는 nginx 업스트림을 전환하는 blue-green 방식이라 서비스 중단 없이 이뤄집니다.

## 15. 엔드투엔드 데이터 파이프라인

먼저 핵심 흐름인 처방전 처리 파이프라인입니다. 사용자가 처방전을 촬영하면, 이미지는 서버를 거치지 않고 스토리지에 바로 올라갑니다. 그다음 OCR로 텍스트와 좌표를 뽑고, 개인정보를 가린 뒤, AI가 약품 정보를 정형화된 JSON으로 바꿔 DB에 저장합니다. 원본 이미지는 처리 직후 즉시 삭제해서 민감정보를 남기지 않습니다.

## 16. 클라우드 기반 보안 파일 업로드

방금 '서버를 거치지 않는다'고 했는데, 이게 보안 설계의 핵심입니다. 서버는 5분만 유효한 Presigned URL을 발급하고, 실제 업로드는 클라이언트가 스토리지로 직접 합니다. 덕분에 서버 병목도, 민감정보가 서버에 머무는 일도 없습니다. 또 OCR 결과에서 주민등록번호와 휴대전화번호 두 가지를 정규식으로 찾아내서, AI에 보내는 텍스트에서는 마스킹하고 저장 이미지에서는 그 부분을 검은색으로 덮습니다.

## 17. 데이터베이스 모델링 (ERD)

데이터 모델입니다. 사용자는 시니어와 보호자로 나뉘고, 둘은 케어 관계로 N대1 연결됩니다. 사용자 아래에 처방전과 약이 있고, 약마다 복약 일정과 복약 기록이 달립니다. 복약 기록에는 인증 사진과 AI 판별 결과가 함께 저장되고요. 그리고 탈퇴는 실제 삭제가 아니라 deletedAt만 남기는 소프트 삭제로 처리해서, 보호자가 과거 기록을 계속 볼 수 있게 했습니다.

## 18. 백엔드 API ① 인증/유저

인증은 카카오 OIDC 소셜 로그인을 씁니다. 앱이 받아온 카카오 ID 토큰을 백엔드가 카카오 공개키로 검증한 뒤, 우리 서비스 전용 JWT를 발급합니다. 시니어와 보호자는 고유한 연동 코드로 매칭되고, 모든 요청은 역할 기반으로 권한을 검증합니다.

## 19. 백엔드 API ② 복약/알림

복약과 알림입니다. 스프링 스케줄러가 매분 돌면서, 복약 시간이 되면 FCM으로 알림을 보냅니다. 그리고 정해진 시간 안에 인증이 없으면 — 기본 60분, 집중 모드는 30분 — 보호자에게 지연 알림이 갑니다. 오차 1분 이내로 정밀하게 발송되도록 했습니다.

## 20. 처방전 데이터 파싱 (OCR)

처방전 파싱은 네이버 클로바 OCR을 씁니다. 한국어 인쇄체 인식 정확도가 높아서 선택했고, 단순 텍스트뿐 아니라 글자마다 좌표까지 함께 받습니다. 이 좌표 덕분에 앞에서 본 이미지 마스킹이 가능합니다.

## 21. 비정형 문서의 정형화

OCR이 뽑은 텍스트는 사실 그대로는 지저분합니다. 띄어쓰기가 깨지거나 오인식도 있고요. 이걸 gpt-5.4-nano 모델에 넣어서 약품명·용량·복용 시간대를 깔끔한 JSON으로 바꿉니다. 온도값은 0, JSON 모드를 강제해서 매번 일관되고 바로 파싱 가능한 결과가 나오게 했습니다.

## 22. DUR 2단계 캐싱 및 약품 매칭

약물 안전성 점검, DUR입니다. 외부 식약처 API가 느리고 가끔 장애도 나서 2단계 캐시로 감쌌습니다. 먼저 메모리 캐시를 보고, 없으면 Redis·DB 캐시, 둘 다 없을 때만 외부 API를 호출해 24시간 저장합니다. 약품명은 띄어쓰기와 단위를 정규화해서 비교하고, 못 찾으면 성분명으로 한 번 더 찾습니다.

## 23. AI 모델 및 프롬프트 전략

정리하면 용도별로 세 가지 모델을 씁니다. 처방전 파싱엔 gpt-5.4-nano, 사진 속 알약을 세는 비전엔 gpt-5.4-mini, 대화형 채팅엔 gpt-4o-mini입니다. 채팅은 시니어에 맞춰 쉬운 말과 짧은 문장으로 답하도록 페르소나를 주고, 추측하지 말고 도구를 호출해서 실제 데이터로 답하도록 설계했습니다.

## 24. 복약 사진 비전 검증

복약 인증의 핵심입니다. 시니어가 약 사진을 찍으면 비전 모델이 알약 개수를 세고, 예정 복용량과 비교해서 일치·불일치·판별불가·실패 네 가지로 판정합니다. 그리고 — 사실 이번 발표 직전에 개선한 부분인데 — 개수가 맞지 않으면 복약 완료로 확정하지 않도록 했습니다. 실제로 안 먹었을 수 있는 약이 먹은 걸로 잘못 기록되는 걸 막은 거죠.

## 25. 실제 서비스 시연

마지막으로 실제 시연입니다. 사진처럼 용량이 큰 입력은 클라이언트에서 압축해 올리고, AI 응답은 스트리밍으로 토큰 단위로 내려보내서 기다리는 체감을 줄였습니다.

그럼 실제 동작을 보여드리겠습니다.
**(시연: 처방전 등록 → 복약 알림 → 사진 인증 → 음성 상담)**

이상으로 기술 구현 파트를 마치겠습니다. 감사합니다.

---

### ⚠️ 발표 전 확인
- **슬라이드 25**: 원안의 "비동기 처리 도입"은 코드에서 확인되지 않아, 검증된 사실(**클라이언트 측 이미지 압축 + 응답 스트리밍**) 위주로 작성함. 서버 비동기 처리를 강조하려면 근거 확인 필요.
- 모델명(gpt-5.4-nano/mini, gpt-4o-mini), 마스킹 2종, 지연 임계치 60/30분, blue-green 무중단 배포 등은 모두 코드 대조로 확인된 사실.
